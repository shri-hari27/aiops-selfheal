apiVersion: v1
kind: Service

metadata:
  name: aiops-selfheal-service
  namespace: aiops

spec:

  type: NodePort

  selector:
    app: aiops-selfheal

  ports:

  - port: 8000

    targetPort: 8000

    nodePort: 30080
